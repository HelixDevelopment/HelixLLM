# HelixLLM Phase-4 video-gen service Python deps — PINNED (§11.4.6 no-float).
#
# torch/torchvision are pulled from the cu128 index (see Containerfile
# --index-url); the cu128 wheels carry native Blackwell sm_120 kernels
# (torch >= 2.7.0 was the first stable line with them — §11.4.150 research).
# Target the current line; NEVER downgrade below 2.7 (sm_120 would PTX-JIT).
torch==2.8.0
torchvision==0.23.0

# WAN 2.2 + LTX-Video serving stack. diffusers exposes WanPipeline /
# WanImageToVideoPipeline (Wan-AI/Wan2.2-*) and LTXPipeline / LTXConditionPipeline
# (Lightricks/LTX-Video); transformers hosts the T5/UMT5 text encoder (quantized
# + CPU-offloaded in the min-mem 480p config); ftfy is WAN's prompt-normaliser.
diffusers==0.35.1
transformers==4.57.1
accelerate==1.11.0
safetensors==0.6.2
sentencepiece==0.2.0
protobuf==6.32.1
huggingface-hub==0.35.3
ftfy==6.3.1
Pillow==11.4.0

# Frame -> MP4 muxing for the /v1/videos/generations response
# (diffusers.utils.export_to_video uses imageio + the bundled ffmpeg plugin).
imageio==2.37.0
imageio-ffmpeg==0.6.0

# Minimal HTTP shim.
fastapi==0.121.1
uvicorn==0.38.0
pydantic==2.12.3
